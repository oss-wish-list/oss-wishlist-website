name: CI Build (Stubbed)

on:
  pull_request:
    branches: [ astro ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI build temporarily stubbed. Enable full build by restoring workflow contents."
