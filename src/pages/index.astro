---
import Layout from '../components/Layout.astro';
import { getCollection } from 'astro:content';

const services = await getCollection('services');
const featuredServices = services.filter(service => service.data.featured);
const maintainerServices = services.filter(service => 
  service.data.target_audience === 'maintainer' || service.data.target_audience === 'both'
);
const companyServices = services.filter(service => 
  service.data.target_audience === 'company' || service.data.target_audience === 'both'
);
---

<Layout title="OSS Wishlist - Get the help your project needs">
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-gray-100 to-gray-200 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="max-w-4xl mx-auto">
        <h1 class="text-5xl font-bold text-gray-900 mb-6">
          Creating a <span class="text-emerald-600">sustainable</span> and <span class="text-blue-600">thriving</span> open source ecosystem, one wish at a time
        </h1>
        <p class="text-xl text-gray-600 mb-8">
          Open source maintainers need help. Open Source Wishlist connects maintainers with services and resources that make all the difference
        </p>
        
        <!-- Audience Selection -->
        <div class="grid md:grid-cols-3 gap-6 mb-8">
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Maintainers</h3>
            <p class="text-gray-600 mb-4 flex-grow">Address the critical risks and sustainability gaps in your project. Get targeted support for governance, security, funding strategy, and community health.</p>
            <a href={`${import.meta.env.BASE_URL}/maintainers`} class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Create or update your wishlist
            </a>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Ecosystem Guardians</h3>
            <p class="text-gray-600 mb-4 flex-grow">Create lasting, and verifiable impact on the health and sustainability of your dependencies and open source ecosystem.</p>
            <a href={`${import.meta.env.BASE_URL}/companies`} class="bg-slate-600 text-white px-6 py-3 rounded-lg hover:bg-slate-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Fulfill a wish
            </a>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Open Source Practitioners</h3>
            <p class="text-gray-600 mb-4 flex-grow">Address the gaps that matter most in open source sustainability. Get matched with meaningful, paid work that puts your specialized skills to use.</p>
            <a href={`${import.meta.env.BASE_URL}/practitioners`} class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Join as a practitioner
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How it Works -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">How it works</h2>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">1</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Create your wishlist</h3>
          <p class="text-gray-600">Select the services and resources your project most needs to reach a sustainable operating level</p>
        </div>
        
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">2</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Get support</h3>
          <p class="text-gray-600">Ecosystem guardians may fulfill one or more of your identified wishes</p>
        </div>
        
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">3</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Participate and give feedback</h3>
          <p class="text-gray-600">Participate in the process and give feedback</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Top Ecosystem Guardians -->
  <section class="py-16 bg-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Top Ecosystem Guardians</h2>
        <p class="text-gray-600">Those who have contributed to wishlists so far. Thank you!</p>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-8 items-center justify-items-center">
        <!-- Placeholder logos -->
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-600 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian A
          </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-700 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian B
          </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-500 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian C
          </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-800 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian D
          </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-600 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian E
          </div>
        </div>
        
        <div class="bg-white p-6 rounded-lg shadow-sm border flex items-center justify-center h-20 w-32">
          <div class="bg-gray-700 text-white px-3 py-2 rounded font-semibold text-sm">
            Guardian F
          </div>
        </div>
      </div>
      
      <div class="text-center mt-8">
        <a href={`${import.meta.env.BASE_URL}/ecosystem-guardians`} class="text-gray-600 font-semibold hover:text-gray-700">
          Learn more about our guardians →
        </a>
      </div>
    </div>
  </section>

  <!-- Call to Action -->
  <section class="py-16 bg-gray-700">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-white mb-4">
        Ready to get the help your project needs?
      </h2>
      <p class="text-gray-200 mb-8 text-lg">
        Create your wishlist in minutes and start connecting with supporters who can help your project thrive.
      </p>
      <a href={`${import.meta.env.BASE_URL}/submit`} class="bg-white text-gray-700 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition-colors">
        Create Your Wishlist Now
      </a>
    </div>
  </section>
</Layout>