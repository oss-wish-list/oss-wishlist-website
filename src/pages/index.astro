---
import Layout from '../components/Layout.astro';
import PlaceholderPage from '../components/PlaceholderPage.astro';
import { getCollection } from 'astro:content';

// Check if site is in placeholder mode
const siteMode = import.meta.env.PUBLIC_SITE_MODE || 'full';
const isPlaceholder = siteMode === 'placeholder';

const services = await getCollection('services');
const featuredServices = services.filter(service => service.data.featured);
const maintainerServices = services.filter(service => 
  service.data.target_audience === 'maintainer' || service.data.target_audience === 'both'
);
const companyServices = services.filter(service => 
  service.data.target_audience === 'company' || service.data.target_audience === 'both'
);
---

{isPlaceholder ? <PlaceholderPage /> : (
<Layout title="OSS Wishlist - Get the help your project needs">
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-gray-100 to-gray-200 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="max-w-4xl mx-auto">
        <h1 class="text-5xl font-bold text-gray-900 mb-6">
          Creating a <span class="text-emerald-600">sustainable</span> and <span class="text-blue-600">thriving</span> open source ecosystem, one wish at a time
        </h1>
        <p class="text-xl text-gray-600 mb-8">
          The missing piece between funding and impact  - is people. 
        </p>
        
        <!-- Audience Selection -->
        <div class="grid md:grid-cols-3 gap-6 mb-8">
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Maintainers</h3>
            <p class="text-gray-600 mb-4 flex-grow">Strengthen your project's long-term sustainability with expert human support in security, governance, funding, and community and leadership strategy.</p>
            <a href={`${import.meta.env.BASE_URL}/maintainers`} class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Create or update your wishlist
            </a>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Ecosystem Sponsors</h3>
            <p class="text-gray-600 mb-4 flex-grow">Invest in the health and sustainability of open source projects that power your organization and our shared ecosystem, with measurable, verifiable impact.</p>
            <a href={`${import.meta.env.BASE_URL}/companies`} class="bg-slate-600 text-white px-6 py-3 rounded-lg hover:bg-slate-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Fulfill a wish
            </a>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm border flex flex-col">
            <h3 class="text-xl font-semibold mb-2">For Open Source Practitioners</h3>
            <p class="text-gray-600 mb-4 flex-grow">Join a community dedicated to strengthening open source sustainability through meaningful, paid work that recognizes and values your expertise.</p>
            <a href={`${import.meta.env.BASE_URL}/practitioners`} class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition-colors block text-center min-h-[44px] flex items-center justify-center">
              Join as a practitioner
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How it Works -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">How it works</h2>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">1</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Maintainers create wishlists</h3>
          <p class="text-gray-600">Identifying from our catalogue, critical services and resources required to ensure a sustainable operating level. Lists are public for the first launch, but will transition to private for privacy and security of projects and their communities.</p>
        </div>
        
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">2</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Ecosystem Sponsors step in to fund one or more wishes on a wishlist</h3>
          <p class="text-gray-600">Companies, foundations and individuals contribute to funding wishes.  Funds are managed in [TBD GitHub Sponsors or Open Collective]</p>
        </div>
        
        <div class="text-center">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-gray-700 text-2xl font-bold">3</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Open Source Practioners are dispatched</h3>
          <p class="text-gray-600">Practioners with validated expertise, using standards set by Open Source Wishlist are dispatched to help. They are either hired through our platform, or provided directly by Ecosystem Gaurdians. </p>
        </div>
      </div>
      
      <div class="text-center mt-8">
        <p class="text-lg text-gray-600 font-medium italic">Outcomes are tracked and reported on to improve and track impact</p>
      </div>
    </div>
  </section>

  <!-- Call to Action -->
    <!-- Call to Action -->
</Layout>
)}

</Layout>
)}