---
import Layout from '../components/Layout.astro';
import ServiceCard from '../components/ServiceCard.astro';
import WishlistForm from '../components/WishlistForms.tsx';
import AuthenticatedForm from '../components/AuthenticatedForm.tsx';
import { getCollection } from 'astro:content';

const services = await getCollection('services');
const maintainerServices = services.filter(service => 
  service.data.target_audience === 'maintainer' || service.data.target_audience === 'both'
);

// Prepare services data for the form component
const servicesData = services.map(service => ({
  id: service.slug,
  title: service.data.title,
  description: service.data.description,
  tags: service.data.tags,
  slug: service.slug
}));
---

<Layout title="Services for Open Source Maintainers - OSS Wishlist" description="Practitioner services to help open source maintainers scale projects, build communities, and create sustainable funding.">
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-50 to-indigo-100 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-5xl font-bold text-gray-900 mb-6">
          Services for <span class="text-gray-600">Open Source Maintainers</span>
        </h1>
        <p class="text-xl text-gray-600 mb-8">
          From solo projects to large communities, get practitioner help to scale sustainably, 
          build engaged communities, and create lasting impact.
        </p>
        <div class="flex justify-center space-x-4">
          <button id="get-started-btn-hero" class="bg-gray-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-gray-700 transition-colors">
            Get Started
          </button>
          <a href="#services" class="border border-gray-300 text-gray-700 px-8 py-3 rounded-lg font-semibold hover:bg-gray-50 transition-colors">
            View Services
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Challenges Section -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">Common Maintainer Challenges</h2>
        <p class="text-xl text-gray-600">We understand the unique challenges open source maintainers face</p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center p-6">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Burnout & Financial Strain</h3>
          <p class="text-gray-600">Overwhelming workloads from lack of governance and maintainer onboarding, combined with unclear funding strategies, create unsustainable conditions that discourage long-term project commitment</p>
        </div>
        
        <div class="text-center p-6">
          <div class="bg-red-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Security Vulnerabilities</h3>
          <p class="text-gray-600">Managing security audits, dependency updates, and vulnerability responses requires specialized knowledge that most maintainers lack, leaving projects and their users exposed to risks</p>
        </div>
        
        <div class="text-center p-6">
          <div class="bg-gray-200 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-2">Community Growth</h3>
          <p class="text-gray-600">Without strategic community engagement and effective moderation tools to combat spam and low-quality contributions, projects can struggle with unfocused efforts and chaotic community dynamics</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Expandable Wishlist Form Section -->
  <section id="wishlist-form-section" class="py-16 bg-white hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-4xl font-bold text-gray-900 mb-4">
          Create Your <span class="text-gray-600">Project Wishlist</span>
        </h2>
        <p class="text-xl text-gray-600 max-w-4xl mx-auto mb-8">
          Share what your open source project needs to thrive. From governance help to security audits, 
          funding strategy to community building - let supporters know how they can help make your project sustainable.
        </p>
      </div>

      <!-- Wishlist Form Component -->
      <AuthenticatedForm client:load>
        <WishlistForm client:load services={servicesData} />
      </AuthenticatedForm>
    </div>
  </section>

</Layout>

<script>
  // Handle Get Started button clicks
  document.addEventListener('DOMContentLoaded', function() {
    const formSection = document.getElementById('wishlist-form-section');
    const getStartedBtns = [
      document.getElementById('get-started-btn-hero')
    ].filter(btn => btn !== null);

    function showFormSection() {
      if (formSection) {
        formSection.classList.remove('hidden');
        formSection.scrollIntoView({ 
          behavior: 'smooth',
          block: 'start'
        });
      }
    }

    // Add click handlers to all Get Started buttons
    getStartedBtns.forEach(btn => {
      if (btn) {
        btn.addEventListener('click', showFormSection);
      }
    });
  });
</script>