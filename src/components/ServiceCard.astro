---
export interface Props {
  title: string;
  description: string;
  category: string;
  price_tier: 'low' | 'medium' | 'high' | 'enterprise';
  estimated_hours: string;
  slug: string;
}

const { title, description, category, price_tier, estimated_hours, slug } = Astro.props;

const priceTiers = {
  low: { label: 'Budget', color: 'bg-orange-100 text-orange-800' },
  medium: { label: 'Standard', color: 'bg-orange-200 text-orange-900' },
  high: { label: 'Premium', color: 'bg-red-100 text-red-800' },
  enterprise: { label: 'Enterprise', color: 'bg-gray-100 text-gray-800' }
};
---

<div class="service-card bg-white p-6 rounded-lg shadow-sm border hover:shadow-md transition-shadow">
  <div class="flex justify-between items-start mb-4">
    <div>
      <h3 class="text-lg font-semibold text-gray-900 mb-2">{title}</h3>
      <p class="text-gray-600 text-sm mb-3">{description}</p>
    </div>
  </div>
  
  <div class="flex justify-between items-center mb-4">
    <span class="text-sm text-gray-500">Category: {category}</span>
    <span class="text-sm text-gray-500">Est. {estimated_hours}</span>
  </div>
  
  <a 
    href={`${import.meta.env.BASE_URL}/services/${slug}`} 
    class="inline-block bg-orange-600 text-white px-4 py-2 rounded hover:bg-orange-700 transition-colors text-sm font-medium"
  >
    Learn More
  </a>
</div>
